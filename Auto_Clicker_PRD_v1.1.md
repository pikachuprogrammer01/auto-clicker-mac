# Auto Clicker PRD v1.1

**产品类型：** macOS 桌面效率工具  
**产品定位：** 极简鼠标自动点击工具  
**目标平台：** macOS  
**版本：** MVP v1.1

---

## 1. 产品定位

### 1.1 产品名称

暂定名称：**Auto Clicker**

后续可根据产品视觉与品牌定位重新命名。

### 1.2 一句话定位

> 一款运行于 macOS 菜单栏的轻量级鼠标自动点击工具，用户只需设置鼠标按键、点击间隔和点击次数，将鼠标移动到目标位置后即可通过全局快捷键开始或停止自动点击。

### 1.3 核心价值

产品只解决一个问题：

> 当用户需要在鼠标当前指向的位置重复点击时，替用户持续完成鼠标点击操作，并在鼠标移动后自动跟随。

产品不定位为自动化脚本平台、鼠标宏工具、RPA 工具或桌面自动化平台。

核心体验：

**简单、可控、稳定。**

---

## 2. 目标用户

### 2.1 核心用户

需要重复执行鼠标点击操作的 macOS 用户。

典型场景：

- 重复点击某个按钮
- 重复刷新某个页面
- 重复执行某个固定点击操作
- 长时间进行重复性鼠标操作
- 测试软件中的重复点击行为
- 个人效率场景中的重复点击

### 2.2 用户核心诉求

用户不希望学习复杂的自动化工具，只需要：

```text
移动鼠标
  ↓
停在目标位置
  ↓
设置点击参数
  ↓
按快捷键开始
  ↓
自动点击
  ↓
再次按快捷键停止
```

产品优先保证：

**简单、可控、稳定。**

---

## 3. 核心使用流程

```text
首次启动
   ↓
检查 macOS 辅助功能权限
   ↓
用户授权
   ↓
进入主界面
   ↓
设置点击参数
   ↓
按下全局快捷键
   ↓
每次操作前获取当前鼠标位置
   ↓
开始自动点击
   ↓
移动鼠标时点击位置同步跟随
   ↓
再次按快捷键
   ↓
立即停止
```

### 3.1 点击位置规则

一次连点任务开始后，每次操作都使用执行瞬间的鼠标当前位置。

例如：

```text
X = 500
Y = 300

        ↓

Click(500,300)

        ↓

移动鼠标到 (800, 600)

        ↓

Click(800,600)
Click(800,600)
```

用户移动鼠标后，后续点击自动跟随到新的鼠标位置。

---

## 4. 功能边界

### 4.1 MVP包含

#### 鼠标操作

- 左键单击
- 右键单击
- 左键长按
- 右键长按

#### 点击控制

- 点击间隔
- 点击次数
- 无限点击
- 指定点击次数

#### 点击位置

- 每次点击前获取鼠标当前位置
- 点击位置跟随鼠标移动

#### 控制方式

- 开始
- 停止
- 全局快捷键开始/停止

#### macOS能力

- 辅助功能权限检测
- 辅助功能权限引导
- 菜单栏常驻

#### 配置

- 保存用户设置
- 下次启动恢复设置

---

## 5. 明确不做的功能

MVP明确排除：

| 功能 | 是否开发 |
|---|---|
| 固定坐标输入 | ❌ |
| X/Y 坐标设置 | ❌ |
| 坐标列表 | ❌ |
| 多点击位置 | ❌ |
| 鼠标移动轨迹录制 | ❌ |
| 键盘宏 | ❌ |
| 自定义脚本 | ❌ |
| 定时任务 | ❌ |
| 外部程序启动 | ❌ |
| OCR | ❌ |
| 图片识别 | ❌ |
| 条件判断 | ❌ |
| 循环脚本 | ❌ |
| 云同步 | ❌ |
| 用户账号 | ❌ |
| 服务端 | ❌ |
| 数据库 | ❌ |

### 5.1 边界原则

> 只要功能需要用户编排多个操作步骤，就不属于 Auto Clicker MVP。

---

## 6. 功能需求

## 6.1 鼠标按键

用户可以选择：

```text
鼠标按键

● 左键
○ 右键
```

默认：

**左键**

系统应保存用户上次选择。

---

## 7. 点击模式

提供：

```text
点击类型

● 单击
○ 长按
```

### 7.1 单击

执行：

```text
Mouse Down
    ↓
Mouse Up
```

### 7.2 长按

执行：

```text
Mouse Down
    ↓
保持指定时间
    ↓
Mouse Up
```

长按模式需要额外提供：

```text
长按时间：[ 500 ] ms
```

---

## 8. 点击间隔

用户可以输入：

```text
点击间隔：[ 100 ] ms
```

单位：

**毫秒（ms）**

MVP建议限制范围：

**10ms ～ 60,000ms**

非法输入包括：

- 0
- 负数
- 字母
- 空值
- 超出允许范围的数字

系统禁止使用非法值启动任务，并给出明确提示。

---

## 9. 点击次数

提供两种模式：

```text
点击次数

● 无限
○ 指定次数
```

指定次数：

```text
指定次数：[ 1000 ] 次
```

### 9.1 无限模式

持续执行：

```text
Click
↓
等待
↓
Click
↓
等待
↓
...
```

直到用户停止。

### 9.2 指定次数

例如设置：

```text
1000 次
```

完成第 1000 次点击后：

**自动停止。**

---

## 10. 点击位置

本产品不提供固定坐标编辑功能。

界面不提供：

```text
X：____
Y：____
```

也不提供：

- 选择坐标
- 保存坐标
- 坐标列表

点击位置由鼠标实时决定：

> **每次操作前获取鼠标当前位置**

### 10.1 工作机制

```text
移动鼠标
     ↓
按开始快捷键
     ↓
每次操作前读取当前鼠标位置
     ↓
在当前位置点击
     ↓
鼠标移动后继续跟随点击
```

---

## 11. 全局快捷键

全局快捷键是产品核心交互。

默认快捷键暂定：

**⌥ + ⌘ + C**

功能：

```text
⌥⌘C
 ↓
开始

⌥⌘C
 ↓
停止
```

快捷键必须在其他 macOS App 处于前台时仍然有效。

例如用户正在使用：

- Safari
- Chrome
- Finder
- Excel
- Word

均可以通过快捷键控制连点器。

---

## 12. 快捷键配置

设置中允许用户修改开始/停止快捷键：

```text
开始 / 停止快捷键

[ ⌥⌘C ]
```

点击后进入录入状态：

```text
请按下新的快捷键组合
```

系统需要检测：

- 是否为空
- 是否为合法组合
- 是否与应用内部快捷键冲突
- 是否能够成功注册

无法注册时提示：

> 当前快捷键不可用，请更换组合。

---

## 13. 开始与停止

主界面提供明显的状态按钮。

### 13.1 停止状态

```text
● 已停止

[ 开始点击 ]
```

### 13.2 运行状态

```text
● 正在点击

点击次数：1,283

[ 停止点击 ]
```

运行时停止操作优先级最高。

用户可以通过：

- 主界面停止按钮
- 全局快捷键

停止当前任务。

---

## 14. 菜单栏设计

产品推荐采用：

**菜单栏常驻 + Popover 控制面板**

不需要一直显示完整桌面窗口。

示意：

```text
┌──────────────────────────────┐
│  🖱 Auto Clicker              │
│                              │
│  状态                         │
│  ● 已停止                    │
│                              │
│  鼠标按键                     │
│  [ 左键 ▼ ]                  │
│                              │
│  点击类型                     │
│  [ 单击 ▼ ]                  │
│                              │
│  点击间隔                     │
│  [ 100 ] ms                  │
│                              │
│  点击次数                     │
│  ● 无限                       │
│  ○ 指定 [ 1000 ] 次           │
│                              │
│  快捷键                       │
│  ⌥⌘C                         │
│                              │
│       [ 开始点击 ]            │
└──────────────────────────────┘
```

运行状态：

```text
┌──────────────────────────────┐
│  🖱 Auto Clicker              │
│                              │
│  状态                         │
│  ● 正在点击                   │
│                              │
│  点击次数                     │
│  1,284                        │
│                              │
│  点击位置                     │
│  跟随鼠标                     │
│                              │
│  快捷键：⌥⌘C                 │
│                              │
│       [ 停止点击 ]            │
└──────────────────────────────┘
```

---

## 15. macOS 权限

首次运行必须检查：

**辅助功能权限（Accessibility）**

如果没有权限：

```text
┌────────────────────────────┐
│ 需要辅助功能权限            │
│                            │
│ Auto Clicker 需要辅助功能  │
│ 权限才能执行自动点击。      │
│                            │
│ [ 前往系统设置 ]            │
└────────────────────────────┘
```

用户完成授权后：

```text
✓ 权限已开启
```

进入正常使用状态。

---

## 16. 异常情况

### 16.1 没有辅助功能权限

禁止开始点击。

提示：

> 请先开启辅助功能权限。

### 16.2 点击间隔非法

例如：

```text
-100ms
```

提示：

> 点击间隔必须为 10～60000ms。

### 16.3 点击次数非法

例如：

```text
-1
0
abc
```

提示：

> 点击次数必须为大于 0 的整数。

### 16.4 快捷键冲突

提示：

> 当前快捷键无法使用，请重新设置。

### 16.5 程序正在点击时关闭

程序关闭前：

**停止当前点击任务。**

确保程序退出后不会继续发送鼠标事件。

---

## 17. 状态机

程序内部建议明确维护状态：

```text
┌──────────┐
│  STOPPED │
└────┬─────┘
     │ 开始
     ↓
┌──────────┐
│ STARTING │
└────┬─────┘
     ↓
┌──────────┐
│ RUNNING  │
└────┬─────┘
     │ 停止
     ↓
┌──────────┐
│ STOPPING │
└────┬─────┘
     ↓
┌──────────┐
│  STOPPED │
└──────────┘
```

实际 UI 重点展示：

- 已停止
- 正在点击

---

## 18. 数据存储

本产品不需要服务器。

不需要：

- MySQL
- SQLite
- Redis
- 后端 API

只需要本地保存用户配置。

配置包括：

```text
mouseButton
clickType
interval
clickCount
hotkey
longPressDuration
```

推荐使用 macOS 原生用户偏好设置。

---

## 19. 非功能需求

### 19.1 稳定性

程序连续运行过程中：

- 不得无故停止
- 不得产生无法停止的点击任务
- 不得在停止后继续发送点击事件

### 19.2 停止可靠性

这是最高优先级之一。

要求：

> 用户触发停止后，自动点击必须尽快终止。

即使处于：

- 无限点击
- 大量点击次数
- 极短点击间隔

也必须能够正常停止。

### 19.3 性能

程序应该：

- 常驻菜单栏
- 低 CPU 占用
- 低内存占用
- 不明显影响其他 App 的正常使用

### 19.4 隐私

产品不需要联网。

不上传：

- 鼠标位置
- 用户操作
- 点击数据
- 使用记录

所有配置保存在本地。

---

# 20. MVP 验收标准

## AC-01 左键点击

选择左键并启动连点器。

**预期：**

目标位置持续产生左键点击。

---

## AC-02 右键点击

选择右键并启动。

**预期：**

目标位置持续产生右键点击。

---

## AC-03 点击间隔

设置：

```text
100ms
```

执行连续点击。

**预期：**

点击按照设置的时间间隔持续执行。

---

## AC-04 指定次数

设置：

```text
100 次
```

启动。

**预期：**

执行完成 100 次后自动停止。

---

## AC-05 无限点击

选择：

```text
无限
```

启动。

**预期：**

持续点击，直到用户主动停止。

---

## AC-06 当前鼠标位置

鼠标移动到目标位置：

```text
X1 / Y1
```

按开始快捷键。

之后将鼠标移动到：

```text
X2 / Y2
```

**预期：**

移动前的点击发生在 X1 / Y1，移动后的下一次及后续点击发生在 X2 / Y2。

---

## AC-07 全局快捷键

在其他 App 中按：

```text
⌥⌘C
```

**预期：**

连点器正常启动。

再次按：

```text
⌥⌘C
```

**预期：**

连点器立即停止。

---

## AC-08 权限

未授权辅助功能权限时尝试启动。

**预期：**

不能执行自动点击，并引导用户进入系统设置。

---

## AC-09 停止

无限点击运行状态下按停止。

**预期：**

自动点击停止，不再产生新的点击事件。

---

## AC-10 程序退出

连点器运行过程中退出 App。

**预期：**

自动点击停止，程序正常退出。

---

## AC-11 参数持久化

修改：

```text
右键
500ms
1000次
```

关闭并重新打开。

**预期：**

配置仍然存在。

---

## AC-12 非法参数

输入：

```text
-100ms
abc
0次
```

**预期：**

禁止启动，并给出明确提示。

---

## AC-13 长按

选择长按并设置：

```text
长按时间：500ms
```

启动。

**预期：**

每次操作都按照“按下 → 保持 500ms → 释放”的顺序执行。

---

# 21. MVP版本定义

Auto Clicker MVP由四个核心概念组成：

```text
                 Auto Clicker
                      │
          ┌───────────┴───────────┐
          │                       │
       点击配置                快捷控制
          │                       │
     ┌────┼────┐              开始/停止
     │    │    │
    左键  右键  点击类型
              │
           单击/长按
              │
           点击间隔
              │
           点击次数
              │
          无限/指定次数

                      +

          每次操作获取鼠标位置
                      │
                      ↓
                  跟随鼠标
                      │
                      ↓
                 自动执行点击
```

---

# 22. 产品成功标准

产品不以功能数量作为 MVP 成功标准。

### 22.1 3秒理解

用户打开后能够立即理解：

> 这是一个自动点击工具。

### 22.2 10秒配置

用户可以快速完成：

> 左键 + 100ms + 无限点击。

### 22.3 一次操作开始

用户移动鼠标到目标位置后：

> 按快捷键即可开始。

### 22.4 随时可以停止

无论当前处于哪个 App：

> 使用全局快捷键即可停止。

### 22.5 点击位置可预期

运行过程中位置跟随：

> 每次操作只点击执行瞬间的鼠标当前位置。

---

# 23. 最终产品定义

> Auto Clicker 是一款 macOS 原生极简鼠标自动点击工具。产品通过菜单栏提供参数配置，通过全局快捷键控制任务启停，在每次操作前获取鼠标当前位置，根据用户设定的鼠标按键、点击类型、点击间隔和点击次数在当前位置执行自动点击。

**MVP只解决“重复点击”这一件事。**

不做：

- 固定坐标
- 坐标编辑
- 脚本
- 定时任务
- 外部程序控制
- RPA
- 账号
- 云服务
- 后端

核心链路：

> **配置 → 快捷键开始 → 移动鼠标 → 跟随点击 → 快捷键停止**

---

# 24. 开发优先级

建议按照以下顺序开发：

### P0-1：点击引擎

- CGEvent 鼠标事件
- 左键/右键
- 点击间隔
- 点击次数
- 无限执行
- 停止机制

### P0-2：位置控制

- 每次操作前获取当前鼠标位置
- 鼠标移动后更新点击位置
- 任务结束释放状态

### P0-3：全局快捷键

- 注册快捷键
- 开始/停止
- 快捷键修改
- 冲突处理

### P0-4：macOS权限

- Accessibility 权限检测
- 系统设置跳转
- 权限状态刷新

### P0-5：UI

- 菜单栏 App
- Popover
- 配置表单
- 运行状态
- 点击次数统计

### P0-6：配置持久化

- 保存用户设置
- 启动恢复配置

### P1：增强能力

- 长按
- 更完善的错误提示
- 开机启动
- 随机点击间隔

---

## 25. MVP完成判定

当以下条件全部满足时，Auto Clicker v1.1 MVP可以认为完成：

1. 用户可以选择左键或右键。
2. 用户可以设置点击间隔。
3. 用户可以选择无限点击或指定点击次数。
4. 用户可以选择单击或长按。
5. 用户无需输入 X/Y 坐标。
6. 每次点击前自动获取鼠标当前位置。
7. 任务运行过程中移动鼠标，后续点击位置随之改变。
8. 用户可以通过全局快捷键开始任务。
9. 用户可以通过全局快捷键停止任务。
10. 用户可以在其他 App 中使用全局快捷键。
11. 没有 Accessibility 权限时无法执行点击，并能够获得授权引导。
12. 指定次数完成后能够自动停止。
13. 无限点击能够被可靠停止。
14. App 退出时不会继续产生鼠标点击。
15. 用户配置可以持久化。
16. 非法参数无法启动任务。
17. 产品无需服务器即可完整运行。
18. 产品不包含固定坐标、定时任务、外部程序、自定义脚本等非 MVP 功能。

**最终判断标准：**

> 用户能够在不阅读使用说明的情况下完成“移动鼠标 → 设置参数 → 快捷键开始 → 自动点击 → 快捷键停止”的完整流程，并且整个过程稳定、可控、不会产生明显的误操作。
